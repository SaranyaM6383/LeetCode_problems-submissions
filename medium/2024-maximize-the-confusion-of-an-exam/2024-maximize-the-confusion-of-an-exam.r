            maxf=Math.max(maxf,++count[s.charAt(r)-'A']);
        for(int r=0;r<n;r++){
        int[] count=new int[26];
        int n=s.length();
        int result=0;
            while(r-l+1 > maxf+k){
                --count[s.charAt(l)-'A'];
                l++;
            }