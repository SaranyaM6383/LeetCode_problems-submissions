        return oddStart;
    }
}
        evenEnd.next=null;
        oddEnd.next=evenStart;
        if(evenStart == null) return oddStart;
        if(oddStart == null) return evenStart;
        }
                pos++;
                temp=temp.next;