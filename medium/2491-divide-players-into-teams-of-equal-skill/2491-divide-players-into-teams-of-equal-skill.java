            }
        sum+=(long)skill[left]*skill[right];
            left++;
                return -1;
            if(skill[left]+skill[right]!=target){
        while(left<right){
        int right=n-1;
        int left=0;
        long sum=0;
        int target=skill[0]+skill[n-1];
        right--;
    }
        int n=skill.length;
        }
        return sum;
}