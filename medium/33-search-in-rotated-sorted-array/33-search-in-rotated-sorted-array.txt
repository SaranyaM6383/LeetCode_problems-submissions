        lo = 0; hi = n-1;
        int rot=lo;
        }
            else hi=mid;
            if(nums[mid]>nums[n-1]) lo=mid+1;
        while(lo<=hi){
            int mid=(lo+hi)/2;
            int real=(mid+rot)%n;
            if(nums[real]==target)
            return real;
            if(nums[real]==target)
            return real;