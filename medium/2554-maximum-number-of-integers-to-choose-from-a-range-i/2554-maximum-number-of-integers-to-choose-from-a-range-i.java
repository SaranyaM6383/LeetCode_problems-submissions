            }
            if(sum+i>maxSum){
                break;
                continue;
            if(s.contains(i)){
        for(int i=1;i<=n;i++){
        int sum=0;
        int count=0;
        }
        for(int num:banned){
            s.add(num);
        HashSet<Integer> s=new HashSet<>();
    public int maxCount(int[] banned, int n, int maxSum) {
class Solution {