-1) continue;
                                                                        if(dp[i] == 
                                                            // unreachable index
                                                for(int i = 0; i < n; i++) {
                                        dp[0] = 0;
                                // base case
                        Arrays.fill(dp, -1);