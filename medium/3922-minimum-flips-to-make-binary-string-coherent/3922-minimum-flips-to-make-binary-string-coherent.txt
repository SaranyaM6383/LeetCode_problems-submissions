        if(instability <= k)
            return i;
        }
        
            int instability=prefixMax[i]-suffixMin[i];
        for(int i=0;i<n;i++){
        }
            suffixMin[i]=Math.min(suffixMin[i+1],nums[i]);
        for(int i=n-2;i>=0;i--){
        return -1;
    }
}