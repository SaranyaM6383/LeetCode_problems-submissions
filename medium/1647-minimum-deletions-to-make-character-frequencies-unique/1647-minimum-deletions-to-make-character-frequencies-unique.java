               ans+=freq[i]-newFreq;
              int newFreq = Math.max(0,freq[i+1]-1);
            if(freq[i]>=freq[i+1]){
           for(int i=24;i>=0;i--){
           int ans=0;
           Arrays.sort(freq);
        }
        for(char c:s.toCharArray()){
            freq[c-'a']++;
        int[] freq=new int[26];
               freq[i]=newFreq;
            