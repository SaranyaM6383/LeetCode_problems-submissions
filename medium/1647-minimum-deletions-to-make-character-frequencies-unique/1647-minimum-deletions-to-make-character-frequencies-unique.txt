        }
        Arrays.sort(freq);
        int ans=0;
        for(int i=24;i>=0;i--){
            if(freq[i]>=freq[i+1]){
                int newFreq=Math.max(0,freq[i+1]-1);
                ans+=freq[i]-newFreq;
            freq[c-'a']++;
        for(char c:s.toCharArray()){
                freq[i]=newFreq;
            }
        }
        return ans;