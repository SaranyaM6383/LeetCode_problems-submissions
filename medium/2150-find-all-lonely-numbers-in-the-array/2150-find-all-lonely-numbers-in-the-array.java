                }
                if(map.get(num)==1 && !map.containsKey(num-1) && !map.containsKey
                (num+1)){
                    list.add(num);
            for(int num:nums){
            }
            List<Integer> list=new ArrayList<>();
    public List<Integer> findLonely(int[] nums) {
        for(int num:nums){
            map.put(num,map.getOrDefault(num,0)+1);
        HashMap<Integer,Integer> map=new HashMap<>();
            }
            return list;
    }