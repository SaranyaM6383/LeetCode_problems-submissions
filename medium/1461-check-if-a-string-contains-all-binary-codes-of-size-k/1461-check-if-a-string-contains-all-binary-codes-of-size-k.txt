(s.charAt(i) & 1);
                                                    hash = ((hash << 1) & mask) | 
                                        for (int i = 0; i < s.length(); ++i) {
        int req = 1 << k;
                boolean[] seen = new boolean[req];
                        int mask = req - 1;
                                int hash = 0;
    public boolean hasAllCodes(String s, int k) {
class Solution {