                                                                    if ((val - grid[0]
                                                    for (int val : row) {
                                        for (int[] row : grid) {
                                int min = grid[0][0], max = min;
                        int[] freq = new int[10001];
                int N = n * m;
        i
    public int minOperations(int[][] grid, int x) {
class Solution {
        nt n = grid.length, m = grid[0].length;