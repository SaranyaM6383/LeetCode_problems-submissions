       if(k==mid) return'1';
       if(k<mid)return findKthBit(n-1,k);
       char c=findKthBit(n-1,len-k+1);
    }
       return c=='0'?'1':'0'; 
       int mid=(len+1)/2;
       int len = (1 << n) - 1;
       if(n==1) return '0';
    public char findKthBit(int n, int k) {
class Solution {