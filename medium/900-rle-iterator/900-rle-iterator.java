            }else{
                return encoding[i+1];
                encoding[i]-=n;
            if(n<=encoding[i]){
        for(int i=idx;i<encoding.length;i+=2){
    public int next(int n) {
    
    }
        this.idx=0;
        this.encoding=encoding;